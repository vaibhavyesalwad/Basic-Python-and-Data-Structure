"""Program to list all files in a directory in Python"""
import os               # using os module
os.system('ls')  # os.system runs single linux command and 'ls' command lists all files and folders in current directory
