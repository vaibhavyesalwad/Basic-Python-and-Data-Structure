"""Program to convert an integer to binary keep leading zeros"""
num = 50
print(format(num, "010b"))          # using format function
print(format(num, "012b"))     # total digits 10 & 12 these are right aligned
